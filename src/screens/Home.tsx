import {
    Text,
    View
} from 'react-native';

const Home = (props: any) => {
    return (
        <View>
            <Text>This is home view</Text>
        </View>
    );
};

export default Home;