/**
 * @description Interface for list items. This is used for classrooms, absence reasons, subjects and teachers.
 */
interface listItem {
    id: number;
    label: string;
    labelEn: string;
};

export default listItem;